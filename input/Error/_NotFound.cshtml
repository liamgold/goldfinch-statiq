@model Goldfinch.Models.ViewModels.ErrorViewModel;

@{
    ViewBag.Title = Model.Error.SeoMetaTitle;
    ViewBag.Canonical = string.IsNullOrWhiteSpace(Model.Error.SeoCanonicalUrl) ? Model.CurrentUrl : Model.Error.SeoCanonicalUrl;
    ViewData["SeoData"] = Model.SeoData;
}

<div>
    <h1>@Model.Error.BaseTitle</h1>
    <div>
        @Html.DisplayFor(vm => vm.Error.BodyCopy)
    </div>
</div>